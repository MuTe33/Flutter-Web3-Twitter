name: web3_wave_portal
description: A web3 wave portal using Flutter
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  get_it: ^7.2.0
  http: ^0.13.4
  intl: ^0.17.0
  provider: ^6.0.2
  rxdart: ^0.27.3
  # this is for web only
  flutter_web3: ^2.1.9
  url_launcher: ^6.0.20
  web3dart: ^2.3.5
  web_socket_channel: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
